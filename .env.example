# Configuração do Servidor NFe Proxy
# Copie este arquivo para .env e configure

# Porta do servidor
PORT=3100

# Chave de API para autenticação (gere uma chave segura)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_KEY=sua_chave_api_secreta_aqui

# Ambiente (development, production)
NODE_ENV=production

# IPs permitidos (separados por vírgula, deixe vazio para permitir todos)
# Para produção, configure apenas os IPs do Supabase Edge Functions
ALLOWED_IPS=

# Log Level (error, warn, info, debug)
LOG_LEVEL=info

# Timeout para requisições SEFAZ (ms)
SEFAZ_TIMEOUT=30000
